global:
  nameOverride: emby

defaultPodOptions:
  automountServiceAccountToken: false

controllers:
  main:
    enabled: true
    type: deployment
    replicas: 1
    containers:
      emby:
        image:
          repository: amilys/embyserver
          tag: 4.9.5.0
          pullPolicy: IfNotPresent
        env:
          TZ: Asia/Shanghai
          UID: 0
          GID: 0
          GIDLIST: "33,1000"
        probes:
          liveness:
            enabled: true
            type: TCP
            spec:
              initialDelaySeconds: 120
              periodSeconds: 60
              timeoutSeconds: 1
              failureThreshold: 3
          readiness:
            enabled: true
            type: TCP
            spec:
              initialDelaySeconds: 30
              periodSeconds: 60
              timeoutSeconds: 1
              failureThreshold: 3
          startup:
            enabled: false

service:
  main:
    enabled: true
    primary: true
    controller: main
    ports:
      http:
        enabled: true
        port: 8096

ingress:
  main:
    enabled: true
    annotations:
      helm.sh/resource-policy: keep
      kubernetes.io/tls-acme: "true"
      cert-manager.io/cluster-issuer: ${DOMAIN}-letsencrypt-issuer
    className: "nginx"
    hosts:
      - host: mov.${DOMAIN}
        paths:
          - path: /
            pathType: Prefix
            service:
              name: emby
              port: 8096
    tls:
      - hosts:
          - mov.${DOMAIN}
        secretName: mov.${DOMAIN}-tls

persistence:
  config:
    enabled: true
    forceRename: emby-config
    type: persistentVolumeClaim
    storageClass: ${DEFAULT_STORAGE_CLASS}
    accessMode: ReadWriteOnce
    size: 100Gi
    retain: true
    globalMounts: []
    advancedMounts:
      main:
        emby:
          - path: /config
            readOnly: false
  ext-launchpotplayer:
    enabled: true
    type: custom
    volumeSpec:
      configMap:
        name: emby-ext
    advancedMounts:
      main:
        emby:
          - path: /system/dashboard-ui/embyLaunchPotplayer.js
            subPath: embyLaunchPotplayer.js
            readOnly: true
#  media:
#    enabled: true
#    type: hostPath
#    hostPath: /mnt/media
#    advancedMounts:
#      main:
#        emby:
#        - path: /media
#          readOnly: false
