{% load i18n %}{% load humanize %}{% autoescape off %}你好，{{ user.username }}，

你的邮箱多因素认证验证码如下：
Your email multi-factor authentication code is:

{{ token }}

如果你没有请求此验证码，请忽略此邮件。上方验证码有效期为 {{ expires|timeuntil }}。
If you did not request this code, please ignore this email. The code above is valid for {{ expires|timeuntil }}.

--
Powered by ${BRAND_PREFIX}.
{% endautoescape %}
