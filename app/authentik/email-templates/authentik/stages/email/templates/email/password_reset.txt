{% load i18n %}{% load humanize %}{% autoescape off %}你好，{{ user.username }}，

你最近请求重置 ${BRAND_PREFIX} 账号密码。请打开下方链接设置新密码。

You recently requested to change your ${BRAND_PREFIX} account password. Use the link below to set a new password.

{{ url }}

如果你没有请求重置密码，请忽略此邮件。上方链接有效期为 {{ expires|naturaltime }}。
If you did not request a password change, please ignore this email. The link above is valid for {{ expires|naturaltime }}.

--
Powered by ${BRAND_PREFIX}.
{% endautoescape %}
